defaults:
  - maddpg_config
  - _self_


# Per-agent critics. A shared group Q would regress one prediction against n distinct rewards.
share_param_critic: False
loss_function: "l2"
delay_value: True
use_tanh_mapping: True
