# MLP configuration for Dynamic Anchors environment
# 
# Observation space: (2 * n_features + 2,) 
#   - Lower bounds for each feature: n_features
#   - Upper bounds for each feature: n_features  
#   - Precision: 1
#   - Coverage: 1
#
# Action space: (2 * n_features,)
#   - Lower bound deltas: n_features (in [-1, 1])
#   - Upper bound deltas: n_features (in [-1, 1])

name: mlp

num_cells: [256, 256]
layer_class: torch.nn.Linear

activation_class: torch.nn.ReLU
activation_kwargs: null

norm_class: null
norm_kwargs: null

# This is a dummy value. MLP is built using the observation space.
num_feature_dims: 1
